#include <stdio.h>
/**
 * main - Print hex numbers
 *
 * Return: Returns zero
 */
int main(void)
{
	int i;

	int b;

	int c;

	int d;

	for (i = 0; i < 10; i++)
	{
		b = 1;
		putchar(b);
	}
	for (c = 'a'; c < 'g'; c++)
	{
		d = c;
		putchar(d);
	}
	putchar('\n');
	return (0);
}
