#include <stdio.h>
#include "main.h"
#include <string.h>
/**
 *_strlen - finds the length of a string
 * @s: First operand
 * Return: Returns zero
 */
int _strlen(char *s)
{
	strlen(s);
	return (0);
}
