#include <stdio.h>
#include "main.h"
#include <string.h>
/**
 *_strlen - finds the length of a string
 * @s: First operand
 * Return: Returns string length
 */
int _strlen(char *s)
{
	int n;

	n = strlen(s);
	return (n);
}
