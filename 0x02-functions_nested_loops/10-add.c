#include <stdio.h>
#include "main.h"
/**
 * add - adds 2 ints
 * @x: First operand
 * @y: Second operand
 *
 * Return: Returns an integer
 */
int add(int x, int y)
{
	int res = x + y;

	return (res);
}
