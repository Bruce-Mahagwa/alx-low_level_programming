#include <stdio.h>
#include "main.h"
#include <ctype.h>
/**
 * mul - multiplies 2 integers
 * @a: First operand
 * @b: Second operand
 *
 * Return: Returns the results of 2 numbers being multiplied
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
